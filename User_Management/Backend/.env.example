# Copy this file to .env and fill values. Do NOT commit real secrets.

# Server
PORT=5000
MONGO_URI=mongodb://localhost:27017/user_management
JWT_SECRET=replace_with_a_strong_secret

# Preferred: SendGrid (recommended)
# Sign up at https://sendgrid.com, verify a sender identity, then paste your API key here
SENDGRID_API_KEY=your_sendgrid_api_key_here
SENDGRID_FROM=your_verified_sendgrid_email@example.com

# Alternative: Gmail SMTP (works with App Passwords)
# If you use Gmail, create an App Password and set EMAIL_USER to your Gmail address,
# and EMAIL_PASS to the app password. See instructions below.
EMAIL_USER=your.email@gmail.com
EMAIL_PASS=your_gmail_app_password_here
MAIL_FROM=your.email@gmail.com

# Optional SMTP server (if you run your own SMTP/Mailgun etc)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp_user
SMTP_PASS=smtp_pass
SMTP_SECURE=false

# Tips:
# - Prefer SendGrid for production (stable deliverability).
# - For Gmail, enable 2FA on your account and create an App Password, then use that app password
#   for EMAIL_PASS. Do not use your regular Gmail password.
# - After adding credentials, restart the backend server.
